For each retrieved context:

1. Remove the context.
2. Ask a judge if the answer can still be produced.
3. If yes -> redundant.
4. If no -> required.

Retrieval Efficiency =
required_contexts / total_contexts